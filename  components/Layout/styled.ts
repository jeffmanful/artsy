import styled from 'styled-components';

export const Wrapper = styled.div`
  padding: 1rem;
  .container {
    padding: 4rem 0 2rem 0;
  }
  .title {
    font-size: 24px;
    cursor: pointer;
    font-weight: bold;
  }

  section {
    margin: 0 0 6rem 0;
  }
`;
